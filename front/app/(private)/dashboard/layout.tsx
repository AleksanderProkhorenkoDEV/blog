import React from "react";

// DONT REMOVE, USE IT FOR INTERCEP ROUTES AND DISPLAY MODAL WITH PARALLEL ROUTES
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
    return (
        <>
            {children}
        </>
    )
}